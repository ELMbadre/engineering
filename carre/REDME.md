# Square Pattern

This is a C program that prints a square pattern of asterisks and spaces. The `carre` function takes an integer `nbr` as input and prints a square pattern of asterisks and spaces with `nbr` rows and columns. The `main` function calls the `carre` function with an input of 10, which prints a square pattern of asterisks and spaces with 10 rows and 10 columns. The pattern consists of asterisks on the first and last rows and columns, and spaces in between. The output of this program is:


## Usage

To run the program, compile the source code using a C compiler and run the executable file.

```bash
gcc square_pattern.c -o square_pattern
./square_pattern

